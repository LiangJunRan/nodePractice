var tools = {
    multiply: (...numbers) => {
        let sum = numbers[0];
        for (let index in numbers) {
            sum = sum * numbers[index]
        }
        return sum
    }
}

module.exports = tools


// 在文件中直接使用 require('03_tool-multiply.js') 引入
